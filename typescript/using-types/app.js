// const person: object = {
//   // person이라는 객체의 타입으로 object라고 명시하면 이 객체는
//   name: "Jhon",
//   age: 30,
// };
// console.log(person.name); // Jhon
// person.name = 1;
// console.log(person.name); // 1
// const person: {
//   name: string;
//   age: number;
// } = {
//   // person이라는 객체의 타입으로 object라고 명시하면 이 객체는
//   name: "Jhon",
//   age: 30,
// };
// console.log(person.name); // Jhon
// person.name = 1;
// console.log(person.name); // 1
var person = {
    // person이라는 객체의 타입으로 object라고 명시하면 이 객체는
    name: "Jhon",
    age: 30
};
console.log(person.name); // Jhon
person.name = 1;
console.log(person.name); // 1
